import { Container, Typography } from '@mui/material';

function Leagues() {
    return (
        <Container sx={{ marginTop: '100px', color: '#fff' }}>
            <Typography variant="h4" gutterBottom>Ligas</Typography>
            <Typography variant="body1">Próximamente.</Typography>
        </Container>
    );
}

export default Leagues;
