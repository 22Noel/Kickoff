# Port configurations
FRONTEND_PORT=80
BACKEND_PORT=8000
DB_PORT=5432

# Database configurations (use strong passwords in production)
POSTGRES_USER=your_db_user
POSTGRES_PASSWORD=your_strong_password
POSTGRES_DB=kickoff
DATABASE_URL=postgresql://your_db_user:your_strong_password@db:5432/kickoff

# Environment
NODE_ENV=development
DENO_ENV=development

# Security (use strong secrets in production)
JWT_SECRET=your_jwt_secret_key
SESSION_SECRET=your_session_secret_key

# Note: This is a template file. Create a .env file with real values for your environment.
# Never commit actual secrets or passwords to version control.
