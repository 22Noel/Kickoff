name: Build API Docker Image

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build Docker image (kickoff-api)
        uses: docker/build-push-action@v5
        with:
          context: ./kickoff-api
          file: ./kickoff-api/Dockerfile
          push: false
          tags: kickoff-api:latest

      # Optional: Add steps to push to registry or run tests
      # - name: Log in to DockerHub
      #   uses: docker/login-action@v3
      #   with:
      #     username: ${{ secrets.DOCKERHUB_USERNAME }}
      #     password: ${{ secrets.DOCKERHUB_TOKEN }}
      # - name: Push image
      #   uses: docker/build-push-action@v5
      #   with:
      #     context: ./kickoff-api
      #     file: ./kickoff-api/Dockerfile
      #     push: true
      #     tags: yourdockerhubuser/kickoff-api:latest
